-- Справочник окон до и после патча заполнялся миграцией V12 и читался
-- единственным потребителем - витриной marts.patch_impact, которая
-- разворачивала его через cross join. Витрина удалена в V33, и там же
-- было решено оставить dim_window в ядре как измерение. С тех пор
-- таблица не читается ни кодом, ни dbt, ни витринами, и решение
-- разворачивается: семь строк без потребителя чинить нечего, а их
-- присутствие в схеме заставляет каждого читателя выяснять, кто
-- их использует. Окна вернутся вместе с витриной, если она вернётся.
drop table core.dim_window;
