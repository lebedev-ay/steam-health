# TODO

## Починить

- [x] `plotly` не указан в `requirements.txt`
- [x] Сглаживание считает окно по индексу массива, а не по календарю
- [ ] `fct_review` создан, но не заполняется. Витрины читают `raw`
      напрямую, размерная модель пока не используется
- [ ] Точки перелома не ищутся в последние 7 дней: окно "после"
      упирается в границу данных. Пользователь этого не видит

## Развитие

- [ ] Классификация событий по тексту новости, а не по заголовку.
      Закроет Rust (патчи называются NAVAL UPDATE) и Destiny
      (патчноуты внутри This Week in Destiny)
- [x] Ранжирование событий по значимости — сделано через длину
      патчноута
- [ ] Плотность упоминаний в прессе как отдельный индикатор
      (порог относительный к обычной для игры частоте)
- [ ] Сбор онлайна по расписанию
- [ ] Заполнение мостов жанров и категорий
- [ ] Тегирование отзывов

## Упаковка

- [ ] Скриншот дашборда в README
- [ ] Запись в `decisions.md` про сравнение Metabase / Power BI /
      Plotly: что умеет каждый, где упёрлись, почему в итоге свой

## Отложено осознанно

Здесь то, что выглядит как долг, но пока не нужно.

- **Материализация `patch_impact`.** Представление разворачивает JSON
  на каждый запрос. На текущем объёме (360 тыс. отзывов) работает
  приемлемо. Материализовать, когда начнёт мешать
- **Инкрементальная выкачка отзывов.** Сейчас полная перезакачка
  вручную раз в несколько дней. Инкремент нужен вместе с расписанием,
  расписания пока нет
- **Вынести DSN и `read_games()` в общий модуль.** Копипаста
  в шести файлах. Чинится за вечер, но пока не мешает
- **dbt.** Витрины сейчас в миграциях, это неудобно при правках
  представлений. Переносить имеет смысл вместе с наведением порядка
  в `core`, а не отдельно
- **`debug=True`, валидация параметров в Flask.** Локальное
  приложение для одного пользователя. Станет важно при публикации