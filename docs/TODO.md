# TODO

## Починить

- [x] `plotly` не указан в `requirements.txt`
- [x] Сглаживание считает окно по индексу массива, а не по календарю
- [x] `fct_review` создан, но не заполняется
- [ ] Точки перелома не ищутся в последние 7 дней: окно "после"
      упирается в границу данных. Пользователь этого не видит
- [ ] Витрины переехали в dbt (patch_impact, review_daily,
      review_flat, dim_game_current — см. decisions.md, запись 030),
      но переход наполовину сделан:
      - `collector/load_fct_patch.py` и `web/tasks.py` всё ещё
        вызывают `refresh materialized view` на `marts.patch_impact`.
        Такого объекта больше нет — это обычная таблица, которую
        собирает dbt. Оба вызова сейчас падают с ошибкой
      - миграции в `db/migration/` (начиная с V13) по-прежнему
        создают и меняют эти витрины — теперь ими владеет dbt,
        а миграции остались нетронутыми, то есть один и тот же
        объект описан в двух местах
      - dbt не встроен в docker-compose: `dbt run` запускается
        только руками
- [x] Metabase висел в docker-compose без использования, убран
- [x] После сбора нужно перезапускать `load_fct_review.py`, иначе
      core отстаёт от raw — теперь описано (docs/model.md). Для
      фонового сбора через дашборд (`web/tasks.py`) это уже
      не ручной шаг: `collect_game` вызывает `load_fct_review.py`
      сама. Ручным шаг остаётся только для сбора через CLI
      (`collector/fetch_reviews.py` напрямую)

## Развитие

- [ ] Классификация событий по тексту новости, а не по заголовку.
      Закроет Rust (патчи называются NAVAL UPDATE) и Destiny
      (патчноуты внутри This Week in Destiny)
- [x] Ранжирование событий по значимости — сделано через длину
      патчноута
- [ ] Плотность упоминаний в прессе как отдельный индикатор
      (порог относительный к обычной для игры частоте)
- [ ] Заполнение мостов жанров и категорий
- [ ] Тегирование отзывов
- [ ] Тематический анализ отзывов через LLM отложен осознанно:
      требует разметки, мостовой таблицы с версией модели
      и десятков тысяч запросов к API. Для цели проекта
      (детект поломки патчем) добавляет мало, размывает фокус.
      Более дешёвое и полезное применение LLM — классификация
      новостей по телу, там объём в сотни раз меньше
- [ ] У Terraria (105600) и Stardew Valley (413150) собрано всего
      около 3000 отзывов каждая — один прогон, заметно меньше
      остальных игр (там обычно ~50 тыс.). Досбор отложен
      осознанно, не забыт
- [ ] `/api/collect` не защищён от повторной постановки задачи
      на ту же игру. Не чиним осознанно: один пользователь,
      а конфликт при реальной гонке ловится ограничением
      `ux_dim_game_current` честной ошибкой транзакции, не тихим
      искажением данных (см. decisions.md, запись 025)

## Упаковка

- [ ] Демо-датасет для быстрого запуска без многочасовой выкачки
- [x] Dockerfile для Flask, чтобы проект поднимался одной командой
- [ ] Деплой на сервер, ссылка в README. Готовность к деплою
      (gunicorn, разовый сервис миграций, restart policy) сделана,
      самого деплоя и публичной ссылки ещё нет
- [x] ER-диаграмма core: нарисована вручную в draw.io
      (docs/er/core.drawio), подключена в model.md вместо mermaid.
      Плюс отдельная диаграмма пайплайна (docs/er/pipeline.drawio)

## Отложено осознанно

Здесь то, что выглядит как долг, но пока не нужно.

- **`load_fct_patch.py` стоило бы фильтровать по источнику.**
  Читает `raw.news` без фильтра по `app_id` и заодно грузит фид
  платформы (appid 753) — 497 строк в `fct_patch` с `game_sk = -1`
  (PCGamesN 298, rps 188, eurogamer 11). На витрины не влияет:
  `patch_impact` теряет их на join к `bounds` (нет отзывов для
  app_id = -1), раньше, чем до любой фильтрации по конкретной
  игре. Строки не мешают, а заглушка `Unknown` на реальных данных
  видна — оставляю как есть
