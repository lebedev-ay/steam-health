# TODO

## Починить

- [ ] Края ряда детект оценивает иначе, чем середину. Сдвиг не считается, пока в окне «до» или «после» меньше трёх дней: на ряду Cyberpunk 2077 в 495 дней это 3 дня в начале и 2 в конце. Хуже другое - дни за 3-7 суток до конца сравнивают полное окно «до» с урезанным «после», то есть меньше наблюдений при том же пороге. В интерфейсе ни то, ни другое не видно
- [ ] В `fct_patch` остались строки с заглушкой `game_sk = -1` от прежней версии загрузчика: 494 из 13 419. Новых не появляется, чистки не было
- [ ] В шаблоне нет `<meta name="viewport">`, поэтому телефон получает страницу в запасной раскладке 980px и ужимает её масштабом. Стенд публичный, с телефона его будут открывать

## Развитие

- [ ] Классифицировать события по тексту новости, а не по заголовку. Сейчас в `patch` попадают 25% событий, 40% остаются `unknown` (запись 004). Закроет Rust, где патчи называются NAVAL UPDATE, и Destiny, где патчноуты лежат внутри This Week in Destiny
- [ ] Плотность упоминаний в прессе как отдельный признак, с порогом относительно обычной для игры частоты
- [ ] Тегирование отзывов

## Отложено осознанно

Здесь то, что выглядит как долг, но пока не нужно.

- **Тематический анализ отзывов через LLM.** Требует разметки, отдельной таблицы связей с версией модели и десятков тысяч запросов к API. Для поиска поломок после патча добавляет мало и размывает задачу. Дешевле применить LLM к классификации новостей по тексту - там объём в сотни раз меньше
- **Пул соединений к базе.** `query()` открывает соединение на каждый вызов, на один `/api/data` их три. При одном пользователе незаметно; понадобится, когда по ссылке начнут ходить

- **Демо-датасет для быстрого запуска.** Задумывался, чтобы можно было посмотреть проект, не собирая данные часами. Эту задачу закрыл живой стенд: смотреть можно ничего не запуская. 
Для развёртывания своей копии проще залить дамп рабочей базы, чем готовить и поддерживать отдельный набор