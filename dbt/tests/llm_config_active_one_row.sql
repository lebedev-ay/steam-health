-- активная конфигурация должна находиться однозначно: ноль строк - в базе нет такой модели и промпта, больше одной - хэш задан слишком коротко
-- или у тех же модели и промпта несколько наборов параметров
select count(*) as n
from {{ ref('llm_config_active') }}
having count(*) <> 1
