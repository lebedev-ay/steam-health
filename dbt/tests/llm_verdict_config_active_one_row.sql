-- конфигурация выводов должна находиться однозначно: ноль строк - вывод этой моделью и промптом ещё не запускался, больше одной - хэш задан слишком коротко
select count(*) as n
from {{ ref('llm_verdict_config_active') }}
having count(*) <> 1
